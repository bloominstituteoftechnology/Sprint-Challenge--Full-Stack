import React, { Component } from 'react';
import axios from 'axios';

const ListAccts = (props) => {
    return (
        <div>
            {/* {props.acctData.map((accts) =>
            <div key={accts._id}>
              <h3>{accts.name} </h3>
                <ul> {accts.description} </ul>
                <ul> {accts.budgetedAmount} </ul>
            </div> */}
            )}
      </div>
    )
}

export default ListAccts
